{% extends "layout.njk" %}

{% block content %}
  <article class="page about-center">
    <h1>{{ title }}</h1>
    <div class="body">
      {{ content | safe }}
    </div>
  </article>
{% endblock %} 