{
  "name": "abakus",
  "version": "1.0.0",
  "description": "portfolio for abakus studio",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "dev": "eleventy --serve",
    "studio": "cd sanity && npm run dev",
    "studio:build": "cd sanity && npm run build"
  },
  "dependencies": {
    "@portabletext/to-html": "^3.0.0",
    "sanity": "^5.8.1"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "fdir": "^6.5.0",
    "gray-matter": "^4.0.3"
  }
}
